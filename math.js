// Mathematical Computation

// ADDITION .

const add = (num1, num2) => {
  // Add num1 to num 2 and store it in a variable sum
  let sum = num1 + num2;

  // Return the sum
  return sum;
};

// SUBTRACTION .

const subtract = (num1, num2) => {
  // Subtract num1 from num 2 and store it in a variable output
  let output = num2 - num2;

  // Return the output
  return output;
};

// REMAINDER .
const remainder = (num1, num2) => {
  // Find the remainder of num1 divided by num2
  let rem = num1 % num2;

  // Return the sum
  return rem;
};

// MULTIPLICATION .
const multiply = (num1, num2) => {
  /* TODO: Implement multiplication */
};
